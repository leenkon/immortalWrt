# 动态生成文件（diy.sh after 阶段生成，不提交）
files/*/etc/uci-defaults/
files/*/etc/shadow
files/*/etc/firstboot-pkgs/profile

# 动态下载文件（构建时下载，不提交）
files/*/etc/openclash/core/
files/*/usr/bin/AdGuardHome

# 离线 .apk 包（apps/ 中的不入库，避免大二进制；含自编译或本地上传的预编译包）
apps/*.apk

# 构建输出
bin/
build_dir/
staging_dir/
tmp/
log/
workflow_*.json

# 编辑器
.workbuddy/
.vscode/
.idea/
*.swp
*.swo
