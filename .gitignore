# 动态生成文件（diy.sh after 阶段生成，不提交）
files/etc/uci-defaults/
files/etc/shadow
files-fanchmwrt/etc/uci-defaults/
files-fanchmwrt/etc/shadow
files-fanchmwrt/etc/firstboot-pkgs/profile

# 动态下载文件（构建时下载，不提交）
files/etc/openclash/core/
files/usr/bin/AdGuardHome

# 自编译离线包（apps/ 中的 .apk 不入库，避免大二进制）
apps/*.apk

# 构建输出
bin/
build_dir/
staging_dir/
tmp/
log/
workflow_*.json

# 编辑器
.workbuddy/
.vscode/
.idea/
*.swp
*.swo
