# FanchmWrt 首启在线安装 —— 通用包（官方 OpenWrt 源，仅 userspace，不含 kmod）
# 每行一个包名；# 开头为注释。kmod 必须编译进镜像（见 build.sh / workflow 内联最小种子 + make defconfig），不要放这里。
# 基础 Web 管理（luci 框架 + 中文 + 默认主题）：defconfig 不默认带 LuCI，首启联网后安装，否则无 Web 界面。
luci
luci-i18n-base-zh-cn
luci-theme-bootstrap
luci-app-ddns
ddns-scripts
ddns-scripts-aliyun
luci-app-upnp
miniupnpd
luci-app-wol
wake-on-lan
luci-app-udpxy
udpxy
luci-app-vlan
openssl-util
coreutils
