# 2026-06-27 工作日志

- 再次审查 diy.sh 逻辑与冗余
- 移除 UTF-8 BOM（第1行 shebang 问题）
- 清理 bypass 段多余空行
- 精简 AdGuardHome.yaml，修正 filter 列表 URL 为官方 AdGuard DNS filter
- 验证 main/bypass 配置生成与语法均通过
- 删除 `system.ntp.enable_server='1'`，路由器不再对外提供 NTP 服务
- 确认所有 config 均使用 fw4/nftables（firewall4=y, nftables=y），无 iptables
- 重写 DNS 劫持脚本为纯 nftables 方案（移除 iptables 回退）
- nftables 规则写入 /etc/dns-hijack.sh，通过 fw4 include（reload=1）在每次防火墙重启时执行
- firewall include 改用命名 section（firewall.dns_hijack_include），避免重复添加
- set -eu 改为 set -e（set -u 导致 sec 等变量 unbound variable 错误）
- main/bypass 配置生成与语法检查均通过
