# 2026-06-28 工作日志

## AdGuardHome 完整修复（多轮排查）

### 1. YAML schema 错误（已修复）
- 根因：`bootstrap_dns` 的 `- 1.1.1.1:53` 未引号包裹，YAML 解析为 mapping 而非 string
- 多次试错：`filter_lists`→`filters`、`bootstrap_dns`→`bootstraps`、duration 字段需带单位、`blocked_services` 格式、缺 `schema_version: 34` 导致 migrator 反复升级失败
- 最终修复：查 AdGuardHome v0.107.76 源码确认正确 schema，写出完整 v34 配置

### 2. 配置文件路径错误（已修复）
- 旧路径：`files/etc/AdGuardHome/AdGuardHome.yaml`（大写）
- 正确路径：`files/etc/adguardhome/adguardhome.yaml`（小写）
- OpenWrt adguardhome 包 uci 默认 `config_file='/etc/adguardhome/adguardhome.yaml'`

### 3. DNS 劫持脚本语法错误（已修复 diy.sh）
- `nft add chain` 的 `{}` 未加引号 → shell 解析报 `unexpected "}"` → 加单引号 `'{ type nat hook prerouting priority -100; }'`
- `firewall include reload='1'` → fw4 不支持 → 删除
- `counter` 关键字位置导致 nft 规则加载失败 → 删除

### 4. AdGuardHome 上游 DNS 超时（已修复）
- 症状：AdGuardHome 查询 1.1.1.1:53 / 223.5.5.5:53 全部 i/o timeout，但 ping 正常
- 根因：旁路由 OpenClash 劫持了出站 UDP 53，AdGuardHome 上游查询被拦截
- 修复：AdGuardHome upstream_dns 改用 DoT（DNS over TLS，端口 853）
  - `tls://1.1.1.1` 和 `tls://223.5.5.5`，绕过端口 53 劫持

### 5. 客户端 DNS 未走旁路由（已修复）
- 根因：主路由无 DNS 劫持规则，手动设 DNS 的客户端绕过 AdGuardHome
- 修复：diy.sh 主路由分支生成 `/etc/dns-hijack.sh`，nftables redirect :53 到本地 dnsmasq，dnsmasq server=10.10.10.2 转发到 AdGuardHome
- 排除 `saddr 10.10.10.2` 避免旁路由自身回环

## 网络设置审查与修正（第2轮）

### 审查结论
- ip_forward：主路由由 fw4 自动启用，旁路由 sysctl.conf 显式写入 — 均正确
- DNS 链路：DHCP 下发 → dnsmasq strictorder+fallback → AdGuardHome DoT — 完整
- 53 端口劫持：nftables prerouting redirect，IPv4 排除旁路由 — 正确

### 修正项
1. **删除主路由 WAN_FW.forward='ACCEPT'**：WAN zone forward 默认 DROP 是正确安全姿态，改为 ACCEPT 无实际需求；DDNS 走 output 链不受影响
2. **添加 IPv6 DNS 劫持规则**：原规则仅覆盖 IPv4（`ip saddr`），IPv6 DNS 可绕过 AdGuardHome；新增 `ip6 nexthdr udp/tcp dport 53 redirect to :53`；IPv6 不排除旁路由（AdGuardHome 上游走 DoT:853，不经 53，无死循环风险）
3. **补上 `firewall.dns_hijack_include.enabled='1'`**：显式启用 firewall include
4. **显式设 adguardhome UCI config_file+workdir**：UCI 默认 config_file 是 `/etc/AdGuardHome/AdGuardHome.yaml`（大写），overlay 文件是小写 `/etc/adguardhome/adguardhome.yaml`，Linux 区分大小写导致 AdGuardHome 读不到 overlay 配置；修复：显式设 `config_file='/etc/adguardhome/adguardhome.yaml'` + `workdir='/etc/adguardhome'`

### Overlay 路径改为大写 AdGuardHome（第二轮修正）
- UCI 默认 `config_file='/etc/AdGuardHome/AdGuardHome.yaml'`（大写），之前 overlay 放小写路径导致不一致，靠显式 UCI 覆盖修正
- 更干净的方案：直接把 overlay 目录改为大写 `files/etc/AdGuardHome/AdGuardHome.yaml`，匹配 UCI 默认值
- 删除 diy.sh 中 `config_file`/`workdir` 显式覆盖，只保留 `enabled='1'`

### AdGuardHome overlay 改为旁路由专属（第三轮修正）
- 从 `files/` 共享 overlay 移除 AdGuardHome YAML → 存到 `configs/adguardhome.yaml`（模板）
- diy.sh bypass 分支：复制模板到 `files/etc/AdGuardHome/AdGuardHome.yaml`
- diy.sh main 分支：`rm -rf "$ADGH_OVERLAY"` + 99-custom.sh 中 `uci set adguardhome.config.enabled='0'` + `/etc/init.d/adguardhome disable`
- 主路由不再包含任何 AdGuardHome overlay 文件，且显式禁用

### 运行诊断发现客户端 DNS 流量未到旁路由
- nft 劫持规则存在、dnsmasq server=10.10.10.2 转发配置正确、AdGuardHome 监听 :::53
- 但 AdGuardHome 只收到 localhost NTP 查询（旁路由自身），客户端流量没到达
- 最可能原因：客户端 Windows DNS 缓存/静态配置未指向 10.10.10.2

## 修改文件清单
| 文件 | 修改内容 |
|------|----------|
| `configs/adguardhome.yaml` | 从 files overlay 移出，作为旁路由专属模板 |
| `scripts/diy.sh` | bypass 分支复制模板到 overlay；main 分支删 overlay；删 config_file/workdir 覆盖；删主路由 AdGuardHome 禁用操作；合并冗余 uci commit firewall；清 BOM/CRLF |
| `files/etc/AdGuardHome/` | 已删除（不再作为共享 overlay） |

### diy.sh 完整审查 + AdGuardHome bind_hosts IPv6 修正
- 全链路 DNS 流路追踪确认：4 条路径（直连旁路由/fallback via 主路由/旁路由宕机直连外部/IPv6 via nft劫持）逻辑正确
- heredoc 嵌套展开验证：`<<EOT` 展开 `$DEF_BYPASS_IP`→`10.10.10.2`，`<< 'HIJACK'` 在运行时不二次展开 ✅
- AdGuardHome YAML `bind_hosts` 缺 IPv6 → 加 `"::"` 确保双栈 DNS 接收

### dns_redirect 根因排查与修复
- 症状：旁路由 AdGuardHome 正常（127.0.0.1 查询有响应），但外部客户端查询 10.10.10.2:53 超时
- 根因：`/etc/config/dhcp` 中 `option dns_redirect '1'`，dnsmasq init 脚本自动注入 nftables 规则 `table inet dnsmasq`：UDP 53 → 5453；dnsmasq 只监听 127.0.0.1:5453，外部查询被丢弃
- 修复：diy.sh 主路由+旁路由分支均加入 `uci set dhcp.@dnsmasq[0].dns_redirect='0'`（第140行和202行）
- 验证：删 `table inet dnsmasq` 后 `nslookup doubleclick.net 10.10.10.2` 立即返回 0.0.0.0（AdGuardHome 正确拦截）
## 2026-06-28 续：旁路由删除 WAN 接口

- diy.sh 旁路由分支：将 `uci set network.wan.proto='none'` 改为 `uci -q delete network.wan || true` + `uci -q delete network.wan6 || true`
- 原因：旁路由只有 LAN，WAN 口存在有风险（物理 WAN 口插线可能拿到 IP 干扰路由）
- 彻底删除比禁用更干净，firewall wan zone 保留但 `.network=''`（匹配不到流量，安全）

## 2026-06-28 续：AdGuardHome YAML 从模板改为静态文件

- 删除 `configs/adguardhome.yaml` 模板文件（冗余）
- AdGuardHome YAML 直接放在 `files/etc/AdGuardHome/AdGuardHome.yaml`（静态文件）
- diy.sh 简化：旁路由构建时保留不动，主路由构建时 `rm -rf` 删除
- 不再需要 `cp configs/adguardhome.yaml → files/etc/AdGuardHome/` 的复制操作
