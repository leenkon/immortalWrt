# 2026-06-26

## immortalWrt diy.sh 收尾修复

- 修复 `network.lan6` 逻辑缺陷：主路由块中先 `delete lan6` 再 `set lan6.proto=static` 会留下一个无 device 的空 interface。删除多余的 `set lan6.proto='static'`，只保留 `delete lan6` 和 `lan.ip6assign='64'`，由 netifd 自动处理 IPv6 委派。
- 完成 BOOTSTRAP 流程：填写 IDENTITY.md、USER.md、SOUL.md，删除 BOOTSTRAP.md。
- 创建项目 MEMORY.md，记录网络拓扑、关键技术决策和已修复错误清单。
- DNS 劫持逻辑审查：确认劫持必要性（IoT 硬编码 DNS 旁路 + 集中 failover 控制），修复死循环 bug（旁路由 AdGuardHome 上游查询被劫持回 dnsmasq → 无限循环）。修复：添加 `! -s 10.10.10.2 ! -d 10.10.10.2` 白名单 exclusion。
- 深度代码审查：发现并修复 `build.sh` 缺少 `CONFIG_FILES=$(TOPDIR)/files`（与 GitHub Actions 不一致，导致本地编译时 files/ 覆盖层可能不生效）。删除陈旧输出文件 `files/etc/uci-defaults/99-custom.sh`（含废弃的 noresolv=1 和旧版 dns-hijack.sh）。经逐行审查，diy.sh 其余逻辑无冗余无错误。
- 清理 `rm -f ...99-ipforward.conf`：sysctl 已合并进 99-custom.sh here-doc，不再生成独立文件，该防御性清理行已删除。
